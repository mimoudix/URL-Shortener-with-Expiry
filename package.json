{
  "name": "URL-Shortener-with-Expiry",
  "version": "1.0.0",
  "scripts": {
    "start": "npx tailwindcss -i ./input.css -o ./output.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.2.7"
  }
}
